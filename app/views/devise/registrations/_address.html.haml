= adr.input :first_name, placeholder: "#{t('users.addrform.firstname')}", label: "#{t('users.addrform.firstname')}"
= adr.input :last_name, placeholder: "#{t('users.addrform.lastname')}", label: "#{t('users.addrform.lastname')}"
= adr.input :address, placeholder: "#{t('users.addrform.address')}", label: "#{t('users.addrform.address')}"
= adr.input :city, placeholder: "#{t('users.addrform.city')}", label: "#{t('users.addrform.city')}"
= adr.input :zip, placeholder: "#{t('users.addrform.zip')}", label: "#{t('users.addrform.zip')}"
= adr.input :country, placeholder: "#{t('users.addrform.selcountry')}", label: "#{t('users.addrform.country')}"
= adr.input :phone, placeholder: "#{t('users.addrform.example')} +355 66 123 4567", label: "#{t('users.addrform.phone')}"
